[project]
name = "stayboost-ai"
version = "0.1.0"
description = "StayBoost AI service (FastAPI) — AI Property Analyzer and growth agents."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# Black owns line length; long copy/string literals are acceptable.
ignore = ["E501"]

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
